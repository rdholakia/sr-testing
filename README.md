# Hence — Round 4 Ask prototypes

Everything needed to build the remaining participant prototypes.

**Start with `BUILD-BRIEF.md`.** It covers the strategy these follow, the rules, the file anatomy, and how to make another one.

| File | What it is |
|---|---|
| `compare.html` | **Prep only — do not deploy.** Embeds all five participants; one link would expose everyone's numbers. |
| ~~compare~~ | **Side-by-side view.** All three prototypes at the same stage, step through with the buttons or arrow keys. Open this to spot differences. |
| `BUILD-BRIEF.md` | How to build one. Read first. |
| `luke-ask.html` | Built. Home upgrade, Portland. |
| `heather-ask.html` | Built. Saving for her son's first car. Cleaner of the two — copy this one. |
| `round4-guide.md` | The moderator guide. Explains what the sessions are testing and why. |
| `participant-recall.md` | All five participants: their question, what landed, what didn't, quotes worth using. |
| `extraction-heather.md` | De-duplicated verbatim record of what Finneas showed Heather. Format to follow for the other participants' extractions. |

**Not in this package, and needed:** the session transcripts for Whitney, Josh and Chad, and the de-duplicated content extractions for each. The transcripts matter more than the extractions — the reframe comes from what the person said, not from what the product showed them.

Open any HTML file directly in a browser. No build step, no dependencies, no network calls.

`compare.html` is self-contained — all three prototypes are embedded inside it, so it works on its own with no sibling files.
